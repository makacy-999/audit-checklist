# 云端同步部署指南

配好之后，所有人的改动实时汇总到同一个 GitHub 仓库，不再需要导出 CSV 合并。
**权限由服务端强制校验**——不是前端提示，是真的拒绝写入。

全程约 10 分钟，只需一个免费的 Cloudflare 账号（不用信用卡）。

---

## 第 1 步：生成 GitHub 令牌（2 分钟）

1. 打开 https://github.com/settings/personal-access-tokens/new
2. Token name 填 `audit-checklist-sync`
3. **Resource owner** 选 `makacy-999`，**Repository access** 选 **Only select repositories** → 勾 `audit-checklist`
4. **Permissions** → Repository permissions → **Contents** 选 **Read and write**（其他一律不动）
5. 点 Generate token，**把生成的 `github_pat_...` 复制出来**（只显示一次）

> 这个令牌只放进 Cloudflare 的环境变量里，**不会出现在网页中**，也不会经过我。

---

## 第 2 步：建 Cloudflare Worker（5 分钟）

1. 注册 https://dash.cloudflare.com/sign-up （免费，无需信用卡）
2. 左侧 **Workers and Pages** → **Create** → **Create Worker**
3. 名字随便填，比如 `audit-sync`，点 **Deploy**
4. 点 **Edit code**，把编辑器里原有内容**全选删掉**
5. 打开本项目里的 **`worker.js`**，全选复制，粘贴进去
6. 点右上角 **Deploy**，确认部署

---

## 第 3 步：填环境变量（2 分钟）

还在 Worker 页面 → **Settings** → **Variables and Secrets** → **Add**：

| 变量名 | 值 | 类型 |
|---|---|---|
| `GITHUB_TOKEN` | 第 1 步生成的令牌 | **Secret**（加密，选这个） |
| `SALT` | 随便一串字符，比如 `shengshi2026` | Secret |
| `GH_OWNER` | `makacy-999` | Text |
| `GH_REPO` | `audit-checklist` | Text |
| `GH_BRANCH` | `main` | Text |
| `DATA_PATH` | `data/audit-state.json` | Text |

填完点 **Deploy**。

> `SALT` 是口令加盐，设了之后就不能再改（改了所有人口令失效）。

---

## 第 4 步：把地址填进页面（1 分钟）

1. 回到 Worker 页面，复制顶部那个 `https://audit-sync.xxxx.workers.dev` 地址
2. 打开 https://makacy-999.github.io/audit-checklist/
3. 点右上角 **⚙**
4. 粘贴地址 → 点**测试连接**（应提示"连接成功"）
5. 点**保存** → 会弹出登录框

---

## 第 5 步：每个人登录

- **姓名**：选自己
- **口令**：第一次填就是**设置**，之后用同一个口令登录。忘了找马宏宇重置。
- **马宏宇** 登录后自动是管理员（服务端 `admins` 名单认定），不需要额外口令

登录成功后右上角会显示 **● 已同步**（绿色）。点旁边的 **↻** 可手动拉最新数据。

---

## 数据存在哪

仓库里的 `data/audit-state.json`，结构：

```json
{
  "v": 3,
  "users": { "阮海贝": "<sha256 口令哈希>", ... },
  "admins": ["马宏宇"],
  "S": { "2.1#0": { "v": "done", "by": "阮海贝", "at": "2026-09-09 21:30" } },
  "N": { ...备注... },
  "D": { ...部门... },
  "LOG": [ { "t": "...", "who": "...", "seq": "009", "field": "状态", "from": "未开始", "to": "已就绪" } ]
}
```

每次提交都会在 GitHub 上产生一条 commit `checklist: <时间>`，**本身就是一份带时间戳的审计留痕**。

---

## ⚠️ 建议：把仓库转成私有

配好之后数据文件里会有同事姓名和口令哈希。虽然是哈希、外人算不出原口令，但**姓名和进度会公开可见**。

建议到 https://github.com/makacy-999/audit-checklist/settings → 最底部 **Change repository visibility** → 转 **Private**。

注意：私有仓库的 GitHub Pages 需要付费，转私有后网页得改用 Cloudflare Pages 托管（同样免费，把仓库连上去即可）。**如果暂时不想折腾，保持公开也能用**，只是别把链接发到外面。

---

## 出问题怎么查

| 现象 | 原因 | 处理 |
|---|---|---|
| 右上角"同步失败"，提示"未配置 GITHUB_TOKEN" | 环境变量没填或没重新 Deploy | 检查第 3 步，改完必须再点一次 Deploy |
| 提示"无法连接" | Worker 地址填错 | 重新复制地址，注意末尾不要带 `/` |
| 提示"口令不正确" | 口令输错 | 找马宏宇在 `data/audit-state.json` 里删掉你名字那一行，重新设置 |
| 提示"服务端拒绝 N 项" | 你改了不是自己负责的条目，或改了别人已改的 | 正常拦截，不是故障 |
| 想去掉云端同步 | ⚙ 里清空地址保存 | 退回本地模式，数据只存本机 |

---

## 本地模式（不配云端也能用）

不填 Worker 地址就是本地模式：数据只存在本人浏览器，权限仍是前端控制（防误改 + 留痕）。
各人填完 **导出 CSV** 发给你，你用管理员身份 **导入合并** 汇总。
